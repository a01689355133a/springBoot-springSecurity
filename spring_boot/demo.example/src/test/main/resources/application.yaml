server:
  port: 8080
  servlet:
    context-path: /hello
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/projectWeb"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
